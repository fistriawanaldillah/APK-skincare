runtime: python39
entrypoint: gunicorn -b :$PORT app:app

instance_class: F2
env_variables:
  FLASK_ENV: production
  SECRET_KEY: "secret123"
  DB_USER: "api_user"
  DB_PASS: "api_user360"
  DB_NAME: "user_account"
  INSTANCE_CONNECTION_NAME: "capstone-project-442222:asia-southeast2:account-db"

beta_settings:
  cloud_sql_instances: "capstone-project-442222:asia-southeast2:account-db"
